# Azure SQL Database Configuration
spring.datasource.url=jdbc:sqlserver://ranger789.database.windows.net:1433;database=ds-project;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
spring.datasource.username=logmaster 
spring.datasource.password=123qweasdzxc?
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Hibernate properties for SQL Server (optional, but good practice)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
# Or for more recent SQL Server versions (check compatibility with your Azure SQL version)
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServer2012Dialect

# If you had ddl-auto set for MySQL, adjust for SQL Server if needed
# spring.jpa.hibernate.ddl-auto=update # or none, create, create-drop